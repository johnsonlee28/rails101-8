<%= simple_form_for @group do |f| %>

<div class="group">
<%= f.input :title, :label => "标题：" %>
</div>

<div class="group">
<%= f.input :description, :label => "叙述：" %>
<div>
  
<%= f.submit "Submit", disable_with: "Submitting..." %>
<% end %>
